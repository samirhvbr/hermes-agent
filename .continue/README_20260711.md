# hermes-agent (fork samirhvbr) — Estado e Pendências

> Snapshot gerado em **11/07/2026**. Em snapshots futuros, crie um novo `README_AAAAMMDD.md` nesta pasta.

## O que é

Fork operacional de [`NousResearch/hermes-agent`](https://github.com/NousResearch/hermes-agent) —
o agente de IA auto-aperfeiçoável da Nous Research (Python 3.11 + `uv`; CLI + gateway de
mensageria). Público, MIT. Clonado em `~/x/hermes-agent`. Objetivo: **usar/operar** o agente
(deploy do gateway num servidor) e **contribuir melhorias de volta** como PR para o upstream.

## Estado do git

- Branch **`main`** (alinhada ao upstream — exceção consciente ao nosso padrão `master`).
- Remotes: `origin` → `samirhvbr/hermes-agent`; `upstream` → `NousResearch/hermes-agent`.
- Sincronizar: `git fetch upstream && git merge --ff-only upstream/main && git push origin main`.

## Feito neste snapshot (padronização do fork — `version.md` 0.1.0)

Camada de fork adicionada **sem clobberar arquivos do upstream**:

- `version.md` — nossa linha downstream (0.1.0), distinta da versão do Hermes (`pyproject.toml`, 0.18.2).
- `CLAUDE.md` — convenções do fork (remotes, versionamento, **dois regimes de commit**: nosso
  `versão - comentário` pt-BR na `main` vs. Conventional Commits em inglês nas branches de PR upstream).
- `README_br.md` — visão pt-BR do fork (arquivo novo).
- `SECURITY_GUIDELINES.md` — segurança **operacional** adaptada ao modelo de ameaça de um agente
  que executa comandos ("o único boundary é o SO"); aponta o `SECURITY.md` do upstream para reporte.
- `deploy.sh` + `docs/fork/deploy.md` — deploy idempotente do **gateway via `docker compose`**,
  com **preflight de ambiente** (padrão canônico REQUIRED/OPTIONAL).
- `.claude/settings.json` — perfil Opus/max/plan, nega leitura de segredos (`~/.hermes`, `.env`, `*.key/*.pem`).
- Banners de fork (delimitados `<!-- FORK-SAMIRHVBR:START/END -->`) no topo de `AGENTS.md` e
  `README.md` — upstream preservado 100% abaixo; blocos removíveis para PRs.
- `docs/fork/` — nossa doc de fork, sem colidir com o `docs/` do produto.

## Pendências / próximos passos

- [ ] **Smoke local:** `scripts/run_tests.sh` verde na nossa máquina (garante paridade com a CI).
- [ ] **Primeiro deploy real:** definir servidor/usuário dedicado, popular `~/.hermes` via
  `hermes setup`, rodar `./deploy.sh` (ajustar `DIR`/branch no topo do script). Ver [docs/fork/deploy.md](../docs/fork/deploy.md).
- [ ] **Notificação de deploy (opcional):** setar `DEPLOY_TELEGRAM_BOT_TOKEN`/`DEPLOY_TELEGRAM_CHAT_ID`.
- [ ] **Primeira melhoria → PR upstream:** quando surgir, ramificar de `upstream/main`, Conventional
  Commits em inglês, sem arquivos de fork.

## Regras que não podem ser esquecidas

- `git pull` **antes** de qualquer alteração.
- **Não** clobberar `AGENTS.md`/`README.md`/`docs/`/`SECURITY.md` do upstream — nossa camada é aditiva.
- Segredos **nunca** no repo — vivem em `~/.hermes`.
- Deploy = **Docker** (isolamento de SO é o boundary real).
